name: CI

on:
  push:
    branches:
    - main
    - feature/*
    tags:
      - '*'

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Format
        run: terraform fmt --check --diff --recursive

  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate
        run: terraform init -backend=false; terraform validate
        working-directory: ./infra/app

  plan:
    runs-on: ubuntu-latest
    needs:
      - validate
    steps:
      - uses: actions/checkout@v4
      - name: plan
        run: terraform workspace select dev; terraform init -backend-config=./dev.s3.tfbackend -reconfigure; terraform plan --var-file dev.terraform.tfvars
        working-directory: ./infra/app